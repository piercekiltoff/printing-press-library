---
description: "Printing Press CLI for Postman Explore. Search and browse the Postman API Network Capabilities include: analytics, category, feedback, networkentity, profile, search, search-all, team. Trigger phrases: 'install postman-explore', 'use postman-explore', 'run postman-explore', 'Postman Explore commands', 'setup postman-explore'."
argument-hint: "<command> [args] | install cli|mcp"
allowed-tools: "Read Bash"
---

Invoke the `pp-postman-explore` skill with the user's arguments: $ARGUMENTS

If the user passes no arguments or just `help`, show `postman-explore-pp-cli --help`. If the user's arguments start with `install`, follow the skill's install flow. Otherwise, map the arguments to the best `postman-explore-pp-cli` command and execute.
