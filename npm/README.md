# @mvanhorn/printing-press

Installer and catalog CLI for Printing Press-generated CLIs.

This package is being introduced in stages. The scaffold currently provides the `pp` entrypoint, registry helpers, Go detection helpers, and skill-install shell-out utilities. The user-facing install/update/list/search/uninstall commands land in follow-up implementation units.
