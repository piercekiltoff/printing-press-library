// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLocationsGetListCmd(flags *rootFlags) *cobra.Command {
	var flagIds string
	var flagName string
	var flagCustomerId string
	var flagStreet string
	var flagUnit string
	var flagCity string
	var flagState string
	var flagZip string
	var flagCountry string
	var flagLatitude float64
	var flagLongitude float64
	var flagActive string
	var flagPage string
	var flagPageSize int
	var flagIncludeTotal bool
	var flagSort string
	var flagCreatedBefore string
	var flagCreatedOnOrAfter string
	var flagModifiedBefore string
	var flagModifiedOnOrAfter string
	var flagExternalDataApplicationGuid string
	var flagExternalDataKey string
	var flagExternalDataValues string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-list <tenant>",
		Short:       "Gets a list of locations",
		Example:     "  servicetitan-crm-pp-cli locations get-list 42",
		Annotations: map[string]string{"pp:endpoint": "locations.get-list", "pp:method": "GET", "pp:path": "/tenant/{tenant}/locations", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tenant/{tenant}/locations"
			path = replacePathParam(path, "tenant", args[0])
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "locations", path, map[string]string{
				"ids":                         fmt.Sprintf("%v", flagIds),
				"name":                        fmt.Sprintf("%v", flagName),
				"customerId":                  fmt.Sprintf("%v", flagCustomerId),
				"street":                      fmt.Sprintf("%v", flagStreet),
				"unit":                        fmt.Sprintf("%v", flagUnit),
				"city":                        fmt.Sprintf("%v", flagCity),
				"state":                       fmt.Sprintf("%v", flagState),
				"zip":                         fmt.Sprintf("%v", flagZip),
				"country":                     fmt.Sprintf("%v", flagCountry),
				"latitude":                    fmt.Sprintf("%v", flagLatitude),
				"longitude":                   fmt.Sprintf("%v", flagLongitude),
				"active":                      fmt.Sprintf("%v", flagActive),
				"page":                        fmt.Sprintf("%v", flagPage),
				"pageSize":                    fmt.Sprintf("%v", flagPageSize),
				"includeTotal":                fmt.Sprintf("%v", flagIncludeTotal),
				"sort":                        fmt.Sprintf("%v", flagSort),
				"createdBefore":               fmt.Sprintf("%v", flagCreatedBefore),
				"createdOnOrAfter":            fmt.Sprintf("%v", flagCreatedOnOrAfter),
				"modifiedBefore":              fmt.Sprintf("%v", flagModifiedBefore),
				"modifiedOnOrAfter":           fmt.Sprintf("%v", flagModifiedOnOrAfter),
				"externalDataApplicationGuid": fmt.Sprintf("%v", flagExternalDataApplicationGuid),
				"externalDataKey":             fmt.Sprintf("%v", flagExternalDataKey),
				"externalDataValues":          fmt.Sprintf("%v", flagExternalDataValues),
			}, nil, flagAll, "", "", "hasMore")
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagIds, "ids", "", "Perform lookup by multiple IDs (maximum 50)")
	cmd.Flags().StringVar(&flagName, "name", "", "Filters by customer's name")
	cmd.Flags().StringVar(&flagCustomerId, "customer-id", "", "Filters by customer ID")
	cmd.Flags().StringVar(&flagStreet, "street", "", "Filters by customer's street")
	cmd.Flags().StringVar(&flagUnit, "unit", "", "Filters by customer's unit")
	cmd.Flags().StringVar(&flagCity, "city", "", "Filters by customer's city")
	cmd.Flags().StringVar(&flagState, "state", "", "Filters by customer's state")
	cmd.Flags().StringVar(&flagZip, "zip", "", "Filters by customer's zip")
	cmd.Flags().StringVar(&flagCountry, "country", "", "Filters by customer's country")
	cmd.Flags().Float64Var(&flagLatitude, "latitude", 0.0, "Filters by customer's latitude")
	cmd.Flags().Float64Var(&flagLongitude, "longitude", 0.0, "Filters by customer's longitude")
	cmd.Flags().StringVar(&flagActive, "active", "", "What kind of items should be returned (only active items will be returned by default) Values: [True, Any, False]")
	cmd.Flags().StringVar(&flagPage, "page", "", "The logical number of page to return, starting from 1")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "How many records to return (50 by default)")
	cmd.Flags().BoolVar(&flagIncludeTotal, "include-total", false, "Whether total count should be returned")
	cmd.Flags().StringVar(&flagSort, "sort", "", "Applies sorting by the specified field: '?sort=+FieldName' for ascending order, '?sort=-FieldName' for descending...")
	cmd.Flags().StringVar(&flagCreatedBefore, "created-before", "", "Return items created before certain date/time (in UTC)")
	cmd.Flags().StringVar(&flagCreatedOnOrAfter, "created-on-or-after", "", "Return items created on or after certain date/time (in UTC)")
	cmd.Flags().StringVar(&flagModifiedBefore, "modified-before", "", "Return items modified before certain date/time (in UTC)")
	cmd.Flags().StringVar(&flagModifiedOnOrAfter, "modified-on-or-after", "", "Return items modified on or after certain date/time (in UTC)")
	cmd.Flags().StringVar(&flagExternalDataApplicationGuid, "external-data-application-guid", "", "Returns location records with external data for a particular GUID")
	cmd.Flags().StringVar(&flagExternalDataKey, "external-data-key", "", "Performs lookup by external data key, 'externalDataValues' must also be provided.")
	cmd.Flags().StringVar(&flagExternalDataValues, "external-data-values", "", "Performs lookup by external data values (maximum 50), 'externalDataKey' must also be provided.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
