// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLeadsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyBookingId int
	var bodyBusinessUnitId int
	var bodyCallReasonId int
	var bodyCampaignId int
	var bodyCustomerId int
	var bodyFollowUpDate string
	var bodyJobTypeId int
	var bodyLeadCity string
	var bodyLeadCountry string
	var bodyLeadCustomerName string
	var bodyLeadEmail string
	var bodyLeadPhone string
	var bodyLeadState string
	var bodyLeadStreet string
	var bodyLeadUnit string
	var bodyLeadZip string
	var bodyLocationId int
	var bodyPriority string
	var bodySummary string
	var bodyTagTypeIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create <tenant>",
		Short:       "Creates a lead",
		Example:     "  servicetitan-crm-pp-cli leads create 42 --summary example-value",
		Annotations: map[string]string{"pp:endpoint": "leads.create", "pp:method": "POST", "pp:path": "/tenant/{tenant}/leads"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("campaign-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "campaign-id")
				}
				if !cmd.Flags().Changed("summary") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "summary")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/tenant/{tenant}/leads"
			path = replacePathParam(path, "tenant", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBookingId != 0 {
					body["bookingId"] = bodyBookingId
				}
				if bodyBusinessUnitId != 0 {
					body["businessUnitId"] = bodyBusinessUnitId
				}
				if bodyCallReasonId != 0 {
					body["callReasonId"] = bodyCallReasonId
				}
				if bodyCampaignId != 0 {
					body["campaignId"] = bodyCampaignId
				}
				if bodyCustomerId != 0 {
					body["customerId"] = bodyCustomerId
				}
				if bodyFollowUpDate != "" {
					body["followUpDate"] = bodyFollowUpDate
				}
				if bodyJobTypeId != 0 {
					body["jobTypeId"] = bodyJobTypeId
				}
				if bodyLeadCity != "" {
					body["leadCity"] = bodyLeadCity
				}
				if bodyLeadCountry != "" {
					body["leadCountry"] = bodyLeadCountry
				}
				if bodyLeadCustomerName != "" {
					body["leadCustomerName"] = bodyLeadCustomerName
				}
				if bodyLeadEmail != "" {
					body["leadEmail"] = bodyLeadEmail
				}
				if bodyLeadPhone != "" {
					body["leadPhone"] = bodyLeadPhone
				}
				if bodyLeadState != "" {
					body["leadState"] = bodyLeadState
				}
				if bodyLeadStreet != "" {
					body["leadStreet"] = bodyLeadStreet
				}
				if bodyLeadUnit != "" {
					body["leadUnit"] = bodyLeadUnit
				}
				if bodyLeadZip != "" {
					body["leadZip"] = bodyLeadZip
				}
				if bodyLocationId != 0 {
					body["locationId"] = bodyLocationId
				}
				if bodyPriority != "" {
					body["priority"] = bodyPriority
				}
				if bodySummary != "" {
					body["summary"] = bodySummary
				}
				if bodyTagTypeIds != "" {
					var parsedTagTypeIds any
					if err := json.Unmarshal([]byte(bodyTagTypeIds), &parsedTagTypeIds); err != nil {
						return fmt.Errorf("parsing --tag-type-ids JSON: %w", err)
					}
					body["tagTypeIds"] = parsedTagTypeIds
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "leads",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyBookingId, "booking-id", 0, "The associated Booking Id to use for creating the Lead. This Booking will be dismissed and a Lead will get created...")
	cmd.Flags().IntVar(&bodyBusinessUnitId, "business-unit-id", 0, "ID of the business unit")
	cmd.Flags().IntVar(&bodyCallReasonId, "call-reason-id", 0, "ID of the call reason. Required if follow up date is not provided.")
	cmd.Flags().IntVar(&bodyCampaignId, "campaign-id", 0, "ID of the campaign")
	cmd.Flags().IntVar(&bodyCustomerId, "customer-id", 0, "ID of the customer")
	cmd.Flags().StringVar(&bodyFollowUpDate, "follow-up-date", "", "Date of when to follow up. Required if call reason ID is not provided.")
	cmd.Flags().IntVar(&bodyJobTypeId, "job-type-id", 0, "ID of job type")
	cmd.Flags().StringVar(&bodyLeadCity, "lead-city", "", "City provided for the lead")
	cmd.Flags().StringVar(&bodyLeadCountry, "lead-country", "", "Country provided for the lead")
	cmd.Flags().StringVar(&bodyLeadCustomerName, "lead-customer-name", "", "Customer name provided for the lead")
	cmd.Flags().StringVar(&bodyLeadEmail, "lead-email", "", "Email provided for the lead")
	cmd.Flags().StringVar(&bodyLeadPhone, "lead-phone", "", "Phone number provided for the lead")
	cmd.Flags().StringVar(&bodyLeadState, "lead-state", "", "State provided for the lead")
	cmd.Flags().StringVar(&bodyLeadStreet, "lead-street", "", "Street provided for the lead")
	cmd.Flags().StringVar(&bodyLeadUnit, "lead-unit", "", "Unit provided for the lead")
	cmd.Flags().StringVar(&bodyLeadZip, "lead-zip", "", "Zip provided for the lead")
	cmd.Flags().IntVar(&bodyLocationId, "location-id", 0, "ID of the location")
	cmd.Flags().StringVar(&bodyPriority, "priority", "", "Priority of the lead")
	cmd.Flags().StringVar(&bodySummary, "summary", "", "Lead summary")
	cmd.Flags().StringVar(&bodyTagTypeIds, "tag-type-ids", "", "List of IDs of tags on the lead")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
