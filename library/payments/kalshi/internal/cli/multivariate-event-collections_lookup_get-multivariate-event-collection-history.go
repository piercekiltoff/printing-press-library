// Copyright 2026 trevin-chow. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newMultivariateEventCollectionsLookupGetMultivariateEventCollectionHistoryCmd(flags *rootFlags) *cobra.Command {
	var flagLookbackSeconds int

	cmd := &cobra.Command{
		Use:     "get-multivariate-event-collection-history <collection_ticker>",
		Aliases: []string{"get"},
		Short:   "Get Multivariate Event Collection Lookup History",
		Example: "  kalshi-pp-cli multivariate-event-collections lookup get-multivariate-event-collection-history example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("lookback-seconds") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "lookback-seconds")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/multivariate_event_collections/{collection_ticker}/lookup"
			path = replacePathParam(path, "collection_ticker", args[0])
			params := map[string]string{}
			if flagLookbackSeconds != 0 {
				params["lookback_seconds"] = fmt.Sprintf("%v", flagLookbackSeconds)
			}
			data, prov, err := resolveRead(c, flags, "lookup", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagLookbackSeconds, "lookback-seconds", 0, "Number of seconds to look back for lookup history. Must be one of 10, 60, 300, or 3600.")

	return cmd
}
