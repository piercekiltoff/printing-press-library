// Copyright 2026 user. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strings"

	"github.com/spf13/cobra"
)

var _ = strings.ReplaceAll // ensure import
var _ = fmt.Sprintf        // ensure import
var _ = io.ReadAll         // ensure import
var _ = os.Stdin           // ensure import
var _ json.RawMessage      // ensure import

func newStingrayGetAboveTheFoldCmd(flags *rootFlags) *cobra.Command {
	var flagPropertyId int
	var flagListingId int
	var flagAccessLevel int

	cmd := &cobra.Command{
		Use:   "get-above-the-fold",
		Aliases: []string{"list"},
		Short: "Get property images, price, and top details",
		Example: "  redfin-pp-cli stingray get-above-the-fold",
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/stingray/api/home/details/aboveTheFold"
			params := map[string]string{}
			if flagPropertyId != 0 {
				params["propertyId"] = fmt.Sprintf("%v", flagPropertyId)
			}
			if flagListingId != 0 {
				params["listingId"] = fmt.Sprintf("%v", flagListingId)
			}
			if flagAccessLevel != 0 {
				params["accessLevel"] = fmt.Sprintf("%v", flagAccessLevel)
			}
			data, err := c.Get(path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagPropertyId, "propertyid", 0, "Property id")
	_ = cmd.MarkFlagRequired("propertyid")
	cmd.Flags().IntVar(&flagListingId, "listingid", 0, "Listing id")
	cmd.Flags().IntVar(&flagAccessLevel, "accesslevel", 3, "Access level")

	return cmd
}
