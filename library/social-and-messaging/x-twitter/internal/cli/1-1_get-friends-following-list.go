// Copyright 2026 dinakar-sarbada. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newV11GetFriendsFollowingListCmd(flags *rootFlags) *cobra.Command {
	var flagIncludeProfileInterstitialType int
	var flagIncludeBlocking int
	var flagIncludeBlockedBy int
	var flagIncludeFollowedBy int
	var flagIncludeWantRetweets int
	var flagIncludeMuteEdge int
	var flagIncludeCanDm int
	var flagIncludeCanMediaTag int
	var flagIncludeExtHasNftAvatar int
	var flagIncludeExtIsBlueVerified int
	var flagIncludeExtVerifiedType int
	var flagIncludeExtProfileImageShape int
	var flagSkipStatus int
	var flagCursor string
	var flagUserId string
	var flagCount int
	var flagWithTotalCount bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:   "get-friends-following-list",
		Aliases: []string{"list"},
		Short: "get friends following list",
		Example: "  x-twitter-pp-cli 1-1 get-friends-following-list",
		Annotations: map[string]string{"pp:endpoint": "1-1.get-friends-following-list", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/1.1/friends/following/list.json"
			data, prov, err := resolvePaginatedRead(cmd.Context(), c, flags, "1-1", path, map[string]string{
				"include_profile_interstitial_type": fmt.Sprintf("%v", flagIncludeProfileInterstitialType),
				"include_blocking": fmt.Sprintf("%v", flagIncludeBlocking),
				"include_blocked_by": fmt.Sprintf("%v", flagIncludeBlockedBy),
				"include_followed_by": fmt.Sprintf("%v", flagIncludeFollowedBy),
				"include_want_retweets": fmt.Sprintf("%v", flagIncludeWantRetweets),
				"include_mute_edge": fmt.Sprintf("%v", flagIncludeMuteEdge),
				"include_can_dm": fmt.Sprintf("%v", flagIncludeCanDm),
				"include_can_media_tag": fmt.Sprintf("%v", flagIncludeCanMediaTag),
				"include_ext_has_nft_avatar": fmt.Sprintf("%v", flagIncludeExtHasNftAvatar),
				"include_ext_is_blue_verified": fmt.Sprintf("%v", flagIncludeExtIsBlueVerified),
				"include_ext_verified_type": fmt.Sprintf("%v", flagIncludeExtVerifiedType),
				"include_ext_profile_image_shape": fmt.Sprintf("%v", flagIncludeExtProfileImageShape),
				"skip_status": fmt.Sprintf("%v", flagSkipStatus),
				"cursor": fmt.Sprintf("%v", flagCursor),
				"user_id": fmt.Sprintf("%v", flagUserId),
				"count": fmt.Sprintf("%v", flagCount),
				"with_total_count": fmt.Sprintf("%v", flagWithTotalCount),
			}, nil, flagAll, "cursor", "", "")
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested.
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&flagIncludeProfileInterstitialType, "include-profile-interstitial-type", 1, "Include profile interstitial type")
	cmd.Flags().IntVar(&flagIncludeBlocking, "include-blocking", 1, "Include blocking")
	cmd.Flags().IntVar(&flagIncludeBlockedBy, "include-blocked-by", 1, "Include blocked by")
	cmd.Flags().IntVar(&flagIncludeFollowedBy, "include-followed-by", 1, "Include followed by")
	cmd.Flags().IntVar(&flagIncludeWantRetweets, "include-want-retweets", 1, "Include want retweets")
	cmd.Flags().IntVar(&flagIncludeMuteEdge, "include-mute-edge", 1, "Include mute edge")
	cmd.Flags().IntVar(&flagIncludeCanDm, "include-can-dm", 1, "Include can dm")
	cmd.Flags().IntVar(&flagIncludeCanMediaTag, "include-can-media-tag", 1, "Include can media tag")
	cmd.Flags().IntVar(&flagIncludeExtHasNftAvatar, "include-ext-has-nft-avatar", 1, "Include ext has nft avatar")
	cmd.Flags().IntVar(&flagIncludeExtIsBlueVerified, "include-ext-is-blue-verified", 1, "Include ext is blue verified")
	cmd.Flags().IntVar(&flagIncludeExtVerifiedType, "include-ext-verified-type", 1, "Include ext verified type")
	cmd.Flags().IntVar(&flagIncludeExtProfileImageShape, "include-ext-profile-image-shape", 1, "Include ext profile image shape")
	cmd.Flags().IntVar(&flagSkipStatus, "skip-status", 1, "Skip status")
	cmd.Flags().StringVar(&flagCursor, "cursor", "-1", "Cursor")
	cmd.Flags().StringVar(&flagUserId, "user-id", "44196397", "User id")
	cmd.Flags().IntVar(&flagCount, "count", 3, "Count")
	cmd.Flags().BoolVar(&flagWithTotalCount, "with-total-count", true, "With total count")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
