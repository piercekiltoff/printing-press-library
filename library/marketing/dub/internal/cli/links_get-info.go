// Copyright 2026 trevin-chow. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLinksGetInfoCmd(flags *rootFlags) *cobra.Command {
	var flagDomain string
	var flagKey string
	var flagLinkId string
	var flagExternalId string

	cmd := &cobra.Command{
		Use:     "get-info",
		Short:   "Retrieve a link",
		Example: "  dub-pp-cli links get-info",
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/links/info"
			params := map[string]string{}
			if flagDomain != "" {
				params["domain"] = fmt.Sprintf("%v", flagDomain)
			}
			if flagKey != "" {
				params["key"] = fmt.Sprintf("%v", flagKey)
			}
			if flagLinkId != "" {
				params["linkId"] = fmt.Sprintf("%v", flagLinkId)
			}
			if flagExternalId != "" {
				params["externalId"] = fmt.Sprintf("%v", flagExternalId)
			}
			data, err := c.Get(path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagDomain, "domain", "", "Domain")
	cmd.Flags().StringVar(&flagKey, "key", "", "Key")
	cmd.Flags().StringVar(&flagLinkId, "link-id", "", "The unique ID of the short link.")
	cmd.Flags().StringVar(&flagExternalId, "external-id", "", "This is the ID of the link in the your database.")

	return cmd
}
