# Product Hunt CLI — Full Dogfood Test Matrix
# Leaf subcommands enumerated from --help tree.
# Each leaf gets: --help (exit 0 + Examples), happy path, JSON fidelity, error path (if takes args).

# root-level leaves (commands with RunE, no subgroup)
agent-context
api
auth status
auth logout
auth set-token
calendar
doctor
export
feedback
help
import
info
list
makers
newsletter
open
outbound-diff
post
profile
recent
search
sync
tagline-grep
today
version
watch
which
workflow
collection
comments
topic
user

# feed subgroup
feed
feed raw
feed refresh

# authors subgroup
authors related

# leaderboard subgroup
leaderboard
leaderboard daily
leaderboard weekly
leaderboard monthly
leaderboard yearly
