// Copyright 2026 aron-prins. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCompaniesFinanceEventsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAgentId string
	var bodyAmountCents int
	var bodyBiller string
	var bodyBillingCode string
	var bodyCostEventId string
	var bodyCurrency string
	var bodyDescription string
	var bodyDirection string
	var bodyEstimated bool
	var bodyEventKind string
	var bodyExecutionAdapterType string
	var bodyExternalInvoiceId string
	var bodyGoalId string
	var bodyHeartbeatRunId string
	var bodyIssueId string
	var bodyModel string
	var bodyOccurredAt string
	var bodyPricingTier string
	var bodyProjectId string
	var bodyProvider string
	var bodyQuantity int
	var bodyRegion string
	var bodyUnit string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:     "create <companyId>",
		Short:   "Record a finance event",
		Example: "  paperclip-pp-cli companies finance-events create example-value --biller example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("amount-cents") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "amount-cents")
				}
				if !cmd.Flags().Changed("biller") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "biller")
				}
				if !cmd.Flags().Changed("event-kind") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "event-kind")
				}
				if !cmd.Flags().Changed("occurred-at") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "occurred-at")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/companies/{companyId}/finance-events"
			path = replacePathParam(path, "companyId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAgentId != "" {
					body["agentId"] = bodyAgentId
				}
				if bodyAmountCents != 0 {
					body["amountCents"] = bodyAmountCents
				}
				if bodyBiller != "" {
					body["biller"] = bodyBiller
				}
				if bodyBillingCode != "" {
					body["billingCode"] = bodyBillingCode
				}
				if bodyCostEventId != "" {
					body["costEventId"] = bodyCostEventId
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyDirection != "" {
					body["direction"] = bodyDirection
				}
				if bodyEstimated != false {
					body["estimated"] = bodyEstimated
				}
				if bodyEventKind != "" {
					body["eventKind"] = bodyEventKind
				}
				if bodyExecutionAdapterType != "" {
					body["executionAdapterType"] = bodyExecutionAdapterType
				}
				if bodyExternalInvoiceId != "" {
					body["externalInvoiceId"] = bodyExternalInvoiceId
				}
				if bodyGoalId != "" {
					body["goalId"] = bodyGoalId
				}
				if bodyHeartbeatRunId != "" {
					body["heartbeatRunId"] = bodyHeartbeatRunId
				}
				if bodyIssueId != "" {
					body["issueId"] = bodyIssueId
				}
				if bodyModel != "" {
					body["model"] = bodyModel
				}
				if bodyOccurredAt != "" {
					body["occurredAt"] = bodyOccurredAt
				}
				if bodyPricingTier != "" {
					body["pricingTier"] = bodyPricingTier
				}
				if bodyProjectId != "" {
					body["projectId"] = bodyProjectId
				}
				if bodyProvider != "" {
					body["provider"] = bodyProvider
				}
				if bodyQuantity != 0 {
					body["quantity"] = bodyQuantity
				}
				if bodyRegion != "" {
					body["region"] = bodyRegion
				}
				if bodyUnit != "" {
					body["unit"] = bodyUnit
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "finance-events",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyAgentId, "agent-id", "", "Agent id")
	cmd.Flags().IntVar(&bodyAmountCents, "amount-cents", 0, "Amount cents")
	cmd.Flags().StringVar(&bodyBiller, "biller", "", "Biller")
	cmd.Flags().StringVar(&bodyBillingCode, "billing-code", "", "Billing code")
	cmd.Flags().StringVar(&bodyCostEventId, "cost-event-id", "", "Cost event id")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "USD", "Currency")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyDirection, "direction", "debit", "Direction")
	cmd.Flags().BoolVar(&bodyEstimated, "estimated", false, "Estimated")
	cmd.Flags().StringVar(&bodyEventKind, "event-kind", "", "Event kind")
	cmd.Flags().StringVar(&bodyExecutionAdapterType, "execution-adapter-type", "", "Execution adapter type")
	cmd.Flags().StringVar(&bodyExternalInvoiceId, "external-invoice-id", "", "External invoice id")
	cmd.Flags().StringVar(&bodyGoalId, "goal-id", "", "Goal id")
	cmd.Flags().StringVar(&bodyHeartbeatRunId, "heartbeat-run-id", "", "Heartbeat run id")
	cmd.Flags().StringVar(&bodyIssueId, "issue-id", "", "Issue id")
	cmd.Flags().StringVar(&bodyModel, "model", "", "Model")
	cmd.Flags().StringVar(&bodyOccurredAt, "occurred-at", "", "Occurred at")
	cmd.Flags().StringVar(&bodyPricingTier, "pricing-tier", "", "Pricing tier")
	cmd.Flags().StringVar(&bodyProjectId, "project-id", "", "Project id")
	cmd.Flags().StringVar(&bodyProvider, "provider", "", "Provider")
	cmd.Flags().IntVar(&bodyQuantity, "quantity", 0, "Quantity")
	cmd.Flags().StringVar(&bodyRegion, "region", "", "Region")
	cmd.Flags().StringVar(&bodyUnit, "unit", "", "Unit")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
