// Copyright 2026 aron-prins. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCompaniesUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyBrandColor string
	var bodyBudgetMonthlyCents int
	var bodyDescription string
	var bodyFeedbackDataSharingConsentAt string
	var bodyFeedbackDataSharingConsentByUserId string
	var bodyFeedbackDataSharingEnabled bool
	var bodyFeedbackDataSharingTermsVersion string
	var bodyLogoAssetId string
	var bodyName string
	var bodyRequireBoardApprovalForNewAgents bool
	var bodySpentMonthlyCents int
	var bodyStatus string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:     "update <companyId>",
		Short:   "Update a company",
		Example: "  paperclip-pp-cli companies update example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/api/companies/{companyId}"
			path = replacePathParam(path, "companyId", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyBrandColor != "" {
					body["brandColor"] = bodyBrandColor
				}
				if bodyBudgetMonthlyCents != 0 {
					body["budgetMonthlyCents"] = bodyBudgetMonthlyCents
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyFeedbackDataSharingConsentAt != "" {
					body["feedbackDataSharingConsentAt"] = bodyFeedbackDataSharingConsentAt
				}
				if bodyFeedbackDataSharingConsentByUserId != "" {
					body["feedbackDataSharingConsentByUserId"] = bodyFeedbackDataSharingConsentByUserId
				}
				if bodyFeedbackDataSharingEnabled != false {
					body["feedbackDataSharingEnabled"] = bodyFeedbackDataSharingEnabled
				}
				if bodyFeedbackDataSharingTermsVersion != "" {
					body["feedbackDataSharingTermsVersion"] = bodyFeedbackDataSharingTermsVersion
				}
				if bodyLogoAssetId != "" {
					body["logoAssetId"] = bodyLogoAssetId
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyRequireBoardApprovalForNewAgents != false {
					body["requireBoardApprovalForNewAgents"] = bodyRequireBoardApprovalForNewAgents
				}
				if bodySpentMonthlyCents != 0 {
					body["spentMonthlyCents"] = bodySpentMonthlyCents
				}
				if bodyStatus != "" {
					body["status"] = bodyStatus
				}
			}
			data, statusCode, err := c.Patch(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "companies",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyBrandColor, "brand-color", "", "Brand color")
	cmd.Flags().IntVar(&bodyBudgetMonthlyCents, "budget-monthly-cents", 0, "Budget monthly cents")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description")
	cmd.Flags().StringVar(&bodyFeedbackDataSharingConsentAt, "feedback-data-sharing-consent-at", "", "Feedback data sharing consent at")
	cmd.Flags().StringVar(&bodyFeedbackDataSharingConsentByUserId, "feedback-data-sharing-consent-by-user-id", "", "Feedback data sharing consent by user id")
	cmd.Flags().BoolVar(&bodyFeedbackDataSharingEnabled, "feedback-data-sharing-enabled", false, "Feedback data sharing enabled")
	cmd.Flags().StringVar(&bodyFeedbackDataSharingTermsVersion, "feedback-data-sharing-terms-version", "", "Feedback data sharing terms version")
	cmd.Flags().StringVar(&bodyLogoAssetId, "logo-asset-id", "", "Logo asset id")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().BoolVar(&bodyRequireBoardApprovalForNewAgents, "require-board-approval-for-new-agents", false, "Require board approval for new agents")
	cmd.Flags().IntVar(&bodySpentMonthlyCents, "spent-monthly-cents", 0, "Spent monthly cents")
	cmd.Flags().StringVar(&bodyStatus, "status", "", "Status")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
